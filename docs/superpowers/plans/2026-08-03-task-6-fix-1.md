# Task 6 Runtime and Trust Boundaries Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ship an executable experiment runtime and linearize result, YAML, publication, registration, cancellation, and task terminal trust boundaries.

**Architecture:** Add a focused `experiments/runtime.py` composition root which reconstructs all runtime services from persisted resolved experiment identity and injected local data boundaries. Move durable publication verification into one shared verifier used by Result, runner recovery, and publication crash recovery. Preserve the seven stages while defining successful publication as the irreversible cancellation commit point.

**Tech Stack:** Python 3.12, SQLAlchemy/SQLite, Polars/PyArrow, existing FactorEngine/UniverseBuilder/BacktestEngine/Worker.

## Global Constraints

- Strict TDD: every production behavior gets a deterministic failing test first.
- Production BaoStock uses `BAOSTOCK_CAPABILITIES`, no PIT enrichment, and fails during VALIDATE before artifact/cache publication.
- The complete provider fixture is offline/synthetic and must use the same production composition root.
- Only shipped market factors move from unavailable `1.0.0` refs to registered `2.1.0`; value/quality remain `1.0.0`.
- Cancellation is accepted only before ARTIFACT_VERIFY publication begins; successful publication makes success win.
- Do not add Task 7 CLI/logging/dashboard behavior or touch unrelated untracked directories.
- Final commit message: `fix: complete experiment runtime and terminal trust boundaries`.

---

### Task 1: Concrete runtime and worker composition root (C1)

**Files:**
- Create: `src/quant_core/experiments/runtime.py`
- Modify: `src/quant_core/experiments/client.py`
- Modify: `src/quant_core/experiments/__init__.py`
- Modify: strategy/config refs in `src/quant_core/strategies/*.py` and `configs/experiments/examples/*.yaml`
- Test: `tests/integration/test_experiment_runtime.py`
- Modify: `tests/integration/test_experiment_client.py`

**Interfaces:**
- Produces `ExperimentRuntimeFactory.__call__(ExperimentRecord) -> PreparedExperimentRuntime`.
- Produces `build_experiment_worker(...) -> Worker` and `build_default_experiment_worker(*, worker_id: str) -> Worker`.
- Strategy identity registry contains real factories, never `object()` sentinels.

- [x] Write tests proving the public factory is absent, old market refs fail exact FactorRegistry resolution, BaoStock fails before cache/artifact creation, and the offline complete provider reaches the real seven-stage chain through spawn.
- [x] Run the C1 tests and record deterministic failures.
- [x] Implement exact persisted config/snapshot/rulebook/strategy parsing, FactorRegistry/FeatureCache/FactorEngine construction, UniverseBuilder/hash, factor compute, SnapshotStrategyRunner/BacktestEngine, finalizer/registry/handler/Worker composition.
- [x] Run C1 tests and related strategy/factor/snapshot tests to green.

### Task 2: Fresh durable Result verification (I1)

**Files:**
- Create: `src/quant_core/experiments/verification.py`
- Modify: `src/quant_core/experiments/client.py`
- Modify: `src/quant_core/experiments/runner.py`
- Test: `tests/integration/test_experiment_client.py`

**Interfaces:**
- Produces `validate_registered_publication(detail) -> ExperimentArtifactPublication`, comparing every DB row with exact publication path/hash/size/schema/row_count and manifest metadata.
- `ExperimentResult` stores query + experiment id and obtains a fresh SUCCEEDED detail on every accessor.

- [x] Add before-handoff and after-handoff DB hash/size/schema/row_count/manifest-metadata tamper tests; verify red.
- [x] Implement the shared complete verifier and query-bound Result accessors; make runner recovery reuse it.
- [x] Run I1 and recovery tests to green.

### Task 3: Handle-bound bounded YAML read (I2)

**Files:**
- Modify: `src/quant_core/experiments/config.py`
- Modify: `tests/unit/experiments/test_runner.py`

**Interfaces:**
- YAML reader opens without following final links, validates root/component/file identities, rejects `fstat().st_size > 1 MiB` before allocation, reads at most `1 MiB + 1`, and revalidates identities after read.

- [x] Add a 16 MiB read-budget probe and barrier-controlled file/parent junction replacement tests; verify red.
- [x] Implement descriptor-bound capped reads and component identity validation.
- [x] Run I2 and config resolution tests to green.

### Task 4: Publication success commit and crash recovery (I3)

**Files:**
- Modify: `src/quant_core/experiments/runner.py`
- Modify: `src/quant_core/tasks/queue.py`
- Modify: `src/quant_core/tasks/worker.py` only if queue semantics alone do not preserve outcome
- Modify: `tests/unit/experiments/test_runner.py`
- Modify: `tests/integration/test_experiment_client.py`
- Modify: `tests/integration/test_task_queue.py`
- Modify: `tests/integration/test_worker_recovery.py`

**Interfaces:**
- `ExperimentArtifactFinalizer.recover(experiment) -> publication | None` deep-validates an existing final publication.
- REGISTER executes without a cancellation precheck after publication succeeds.
- `TaskQueue.finish(SUCCEEDED)` accepts an owner-fenced CANCEL_REQUESTED BACKTEST only when its linked experiment is already SUCCEEDED.

- [x] Add barrier tests for cancellation before publication, after publication/before register, after register/before finish, and publication crash/orphan/retry; verify red.
- [x] Implement the commit policy, queue reconciliation, and existing-publication recovery without overwrite.
- [x] Run I3 plus Task 4/5 queue/worker tests to green.

### Task 5: Final verification, report, and commit

**Files:**
- Create ignored delivery report: `.superpowers/sdd/2026-07-30-experiments-tasks-worker/task-6-fix-1-report.md`

- [x] Run Task 6 focused, Task 4/5, artifact/registry, factor, universe/PIT gates with short basetemps.
- [x] Run `uv run ruff check src tests` and `uv run mypy src`.
- [x] Run strict unfiltered `uv run pytest -q --basetemp=C:/q6f1`.
- [ ] Audit exact diff/status, stage only fix files, commit once with the required message, and write the report with SHA/red-green/concerns.
